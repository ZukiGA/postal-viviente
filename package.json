{
  "name": "blog-bohemio",
  "version": "1.0.0",
  "description": "Personal photography blog — cafes, travels, urban exploration",
  "private": true,
  "scripts": {
    "build": "node scripts/strip-exif.js && node scripts/optimize-images.js && npx quartz build --directory quartz",
    "serve": "cd quartz && npx quartz build --serve",
    "strip-exif": "node scripts/strip-exif.js",
    "optimize-images": "node scripts/optimize-images.js"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "sharp": "^0.33.2"
  },
  "engines": {
    "node": ">=18"
  }
}
